<div class="contents row">
    <%= form_with url: "/tweets/#{@tweet.id}", method: :patch, local: true  do |f| %>
      <h3>
        コメントを編集してください。
      </h3>
      <textarea cols="30" name="text" placeholder="text" rows="10"><%= @tweet.text %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>